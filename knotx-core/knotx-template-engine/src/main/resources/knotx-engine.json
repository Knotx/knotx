{
  "config": {
    "service.name": "template-engine",
    "template.debug": true,
    "services": [
      {
        "path": "/service/mock/.*",
        "domain": "localhost",
        "port": 3000
      }
    ]
  }
}