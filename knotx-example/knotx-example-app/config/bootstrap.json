{
  "configRetrieverOptions": {
    "scanPeriod": 5000,
    "stores": [
      {
        "type": "file",
        "format": "conf",
        "config": {
          "path": "${KNOTX_HOME}/config/application.conf"
        }
      }
    ]
  }
}
